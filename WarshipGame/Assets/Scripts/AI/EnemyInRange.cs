using UnityEngine;

public class EnemyInRange : MonoBehaviour
{
    private GameObject[] _playerShips;
    private float _sphereRadius;

    private void DetectPlayer()
    {
        // if (!Physics.CheckSphere(transform.position, _sphereRadius) && ) return;
        
    }
}
